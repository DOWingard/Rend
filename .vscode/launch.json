{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Attach to Ableton Live",
            "type": "cppvsdbg",
            "request": "attach",
            "processId": "${command:pickProcess}",
            "logging": {
                "engineLogging": true
            }
        }
    ]
}
